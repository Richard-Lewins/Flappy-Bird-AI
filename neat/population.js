class population{
    constructor(numOfPlayers){
        let players = [];
        let species = [];
        
        for(let i = 0;i<numOfPlayers;i++){
            players.push(new player());
        }
        
    }
}